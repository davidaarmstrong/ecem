% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{pooled_unit_tau}
\alias{pooled_unit_tau}
\title{Pool each treated unit's tau_hat across the draws that retained it}
\usage{
pooled_unit_tau(draws, n)
}
\arguments{
\item{draws}{A list of per-draw records, as returned by \code{\link[=run_M_draws]{run_M_draws()}}.}

\item{n}{Total number of rows in the original data (i.e. \code{nrow(data)}).}
}
\value{
A numeric vector of length \code{n}, \code{NA} for units never retained in
any draw.
}
\description{
Each treated unit's \code{tau_hat}, averaged across the draws in which it was
retained. Used as the \code{tau(x)} surface for both covariance diagnostics
(\code{\link[=cov_att_ate]{cov_att_ate()}} and \code{\link[=cov_retention_per_draw]{cov_retention_per_draw()}}) -- deliberately the
\emph{pooled} surface, not a per-draw one, since \code{tau(x)} is a fixed
population quantity while \code{r_m(x)} is what actually varies by draw.
}
