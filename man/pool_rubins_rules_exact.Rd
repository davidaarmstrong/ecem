% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{pool_rubins_rules_exact}
\alias{pool_rubins_rules_exact}
\title{Pool exactly-enumerated configurations}
\usage{
pool_rubins_rules_exact(results, weights)
}
\arguments{
\item{results}{A list of per-draw records, as returned by
\code{\link[=run_enumerated_draws]{run_enumerated_draws()}}'s \code{results} element (or \code{\link[=run_M_draws]{run_M_draws()}}'s
output when it enumerated exactly).}

\item{weights}{Numeric vector of weights, one per element of \code{results},
as returned by \code{\link[=enumerate_configs]{enumerate_configs()}}'s \code{weights} element.}
}
\value{
An object of class \code{"ecem_pooled"} (see \code{\link[=print.ecem_pooled]{print.ecem_pooled()}}):
a list with elements \code{tau_bar}, \code{Wbar}, \code{B}, \code{T}, and \code{K}. Unlike
\code{\link[=pool_draws]{pool_draws()}}'s output, \code{lambda_hat}, \code{df}, and \code{exact} are not set
here; \code{\link[=print.ecem_pooled]{print.ecem_pooled()}} fills them in at print time (\code{lambda_hat = B / T}, \code{df = NA}, exact inferred from the presence of \code{K}).
}
\description{
Exact-weight pooling: no \code{(1 + 1/K)} inflation, because that correction
exists specifically to account for not having sampled the full
population of draws -- which, under exact enumeration, has now been
sampled in full. Both \code{tau_bar} and \code{B} are exact, not Monte Carlo
estimates, so \code{T = Wbar + B} with no further adjustment. Prefer
\code{\link[=pool_draws]{pool_draws()}}, which dispatches to this automatically when appropriate.
}
