% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-data.R
\name{simulate_population}
\alias{simulate_population}
\title{Simulate a population for examples and demos}
\usage{
simulate_population(N = 3000, tau0 = 3, heterogeneous = FALSE)
}
\arguments{
\item{N}{Integer; population size.}

\item{tau0}{Numeric; baseline treatment effect (the effect at \code{age = 45} when \code{heterogeneous = TRUE}, or the constant effect otherwise).}

\item{heterogeneous}{Logical; if \code{TRUE}, the treatment effect varies
linearly in \code{age} (\code{tau0 + 0.15 * (age - 45)}); if \code{FALSE}, the effect
is constant at \code{tau0}.}
}
\value{
A data frame with columns \code{age}, \code{income}, \code{educ}, \code{region}
(categorical, 3 levels, for exact-match examples), \code{noise} (unrelated
to treatment or outcome, for excluded-covariate examples), \code{D} (0/1
treatment), and \code{Y} (outcome).
}
\description{
Generates a simple population with a treatment assignment mechanism
depending on \code{age}, \code{income}, \code{educ}, and \code{region}, and an outcome with
either a constant or age-varying treatment effect. Used throughout this
package's examples, demos, and tests; not intended as a general-purpose
data-generating function for real analyses.
}
\examples{
pop <- simulate_population(N = 500, tau0 = 3, heterogeneous = TRUE)
head(pop)

}
