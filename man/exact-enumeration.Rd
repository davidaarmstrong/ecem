% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{exact-enumeration}
\alias{exact-enumeration}
\title{Exact enumeration of achievable coarsenings}
\description{
\code{cut()}-based coarsening only changes at a covariate's own observed
values, so the number of practically distinct outcomes achievable by
varying a cutpoint within an elicited range is finite: if k distinct
observed values fall in \verb{[lo, hi]}, there are exactly k+1 intervals that
produce a distinct binning (before the first value, between each pair,
after the last). Two draws landing in the same interval produce an
\emph{identical} matched sample, not merely a similar one, since the data
doesn't change between draws. Increasing \code{M} past this ceiling doesn't
buy more power -- it just re-estimates a fixed quantity more precisely.
\code{\link[=count_achievable_configs]{count_achievable_configs()}} reports the ceiling before you commit to an
\code{M}; \code{\link[=enumerate_configs]{enumerate_configs()}} / \code{\link[=run_enumerated_draws]{run_enumerated_draws()}} replace Monte
Carlo drawing with exact computation once that ceiling is small enough
to be worth enumerating directly. \code{\link[=run_M_draws]{run_M_draws()}}'s \code{exact_if_K_leq}
argument does this automatically.
}
