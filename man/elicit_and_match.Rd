% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match-and-fit.R
\name{elicit_and_match}
\alias{elicit_and_match}
\title{Resolve cutpoints, coarsen, match, and prune for one draw}
\usage{
elicit_and_match(data, treat_var, cutpoint_specs)
}
\arguments{
\item{data}{A data frame containing \code{treat_var} and every covariate
named in \code{cutpoint_specs}.}

\item{treat_var}{Character; name of the 0/1 treatment indicator column
in \code{data}.}

\item{cutpoint_specs}{A named list, one entry per covariate to include
in the matching, in the format described in
\code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}}.}
}
\value{
A list with elements: \code{retained_idx} (row indices of \code{data}
retained after pruning), \code{stratum} (a factor giving each retained
unit's matched stratum), \code{resolved} (the per-covariate resolution from
\code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}}), \code{kinds} (named character vector of each
covariate's \code{kind}), and \code{regimes} (named character vector, \code{NA} for
non-regime covariates and the drawn regime name for any covariate
specified via \code{\link[=regime]{regime()}} -- lets a run of draws be tabulated by which
theory each one used).
}
\description{
Resolves cutpoints for every covariate in \code{cutpoint_specs} (drawing a
value for elicited/regime covariates, passing fixed covariates through
unchanged), coarsens each one via \code{\link[=coarsen_one]{coarsen_one()}}, forms CEM strata from
their interaction, and prunes strata that lack common support (no
treated or no control units). Does not estimate any effect -- see
\code{\link[=fit_effect]{fit_effect()}} for that.
}
