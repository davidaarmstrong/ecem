% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-methods.R
\name{print.ecem_existence_test}
\alias{print.ecem_existence_test}
\title{Print an existence-test result}
\usage{
\method{print}{ecem_existence_test}(x, alpha = 0.05, digits = 4, ...)
}
\arguments{
\item{x}{An object of class \code{"ecem_existence_test"}.}

\item{alpha}{Significance level for the printed verdict. Defaults to
\code{0.05}.}

\item{digits}{Integer; number of significant digits to print.}

\item{...}{Ignored; present for S3 method consistency.}
}
\value{
\code{x}, invisibly.
}
\description{
Reports the observed statistic, its bootstrap p-value under each draw's
own null, and a verdict at the given \code{alpha}. See \code{\link[=existence_test]{existence_test()}} for
when this test is the right fallback (excess-variance has rejected and
pooling via Rubin's rules is no longer trusted).
}
\details{
Rejecting the null here is evidence that \emph{some} nonzero effect exists
somewhere among the draws -- it is deliberately not evidence for any
single pooled point estimate, since congeniality has already failed and
the draws are no longer treated as interchangeable estimates of one
quantity. Report the individual draws (e.g. \code{\link[=as.data.frame.ecem_draws]{as.data.frame.ecem_draws()}})
descriptively rather than a pooled \code{tau_bar} from here on.
}
\examples{
\donttest{
set.seed(1)
pop <- simulate_population(N = 500)
specs <- list(age = list(c(25, 33), c(60, 68)), educ = c(12, 16))
draws <- run_M_draws(pop, "D", "Y", specs, M = 10)
existence_test(pop, draws, n_boot = 20)
}

}
