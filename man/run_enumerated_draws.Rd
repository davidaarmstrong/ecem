% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{run_enumerated_draws}
\alias{run_enumerated_draws}
\title{Run every achievable configuration exactly}
\usage{
run_enumerated_draws(
  data,
  treat_var,
  outcome_var,
  cutpoint_specs,
  estimator = c("regression", "mean_diff"),
  covariates = NULL
)
}
\arguments{
\item{data}{A data frame containing \code{treat_var} and every covariate
named in \code{cutpoint_specs}.}

\item{treat_var}{Character; name of the 0/1 treatment indicator column
in \code{data}.}

\item{outcome_var}{Character; name of the outcome column in \code{data}.}

\item{cutpoint_specs}{A named list, one entry per covariate to include
in the matching, in the format described in
\code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}}.}

\item{estimator}{\code{"regression"} (the default) or \code{"mean_diff"}; see
Details.}

\item{covariates}{Character vector of covariate names for the
\code{"regression"} estimator, using their raw (uncoarsened) values.
\code{NULL} (the default) uses every non-excluded covariate in \code{matched}
(i.e. \code{names(matched$kinds)} minus any \code{"excluded"} entries) -- the
same covariates that entered the matching. Ignored for
\code{"mean_diff"}.}
}
\value{
A list with elements \code{results} (list of \code{K} per-draw records, as
from \code{\link[=run_draw]{run_draw()}}), \code{weights}, \code{regimes}, and \code{K}.
}
\description{
See \code{?"exact-enumeration"}. Runs \code{\link[=run_draw]{run_draw()}} once per config returned
by \code{\link[=enumerate_configs]{enumerate_configs()}}. Prefer \code{\link[=run_M_draws]{run_M_draws()}} with \code{exact_if_K_leq}
set for the common case of "enumerate exactly if small enough, else fall
back to Monte Carlo" -- call this directly only if you specifically want
exact enumeration regardless of how large \code{K} is.
}
