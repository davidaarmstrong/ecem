% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match-and-fit.R
\name{run_draw}
\alias{run_draw}
\title{Run one draw: elicit, match, and estimate}
\usage{
run_draw(
  data,
  treat_var,
  outcome_var,
  cutpoint_specs,
  estimator = c("regression", "mean_diff"),
  covariates = NULL
)
}
\arguments{
\item{data}{A data frame containing \code{treat_var} and every covariate
named in \code{cutpoint_specs}.}

\item{treat_var}{Character; name of the 0/1 treatment indicator column
in \code{data}.}

\item{outcome_var}{Character; name of the outcome column in \code{data}.}

\item{cutpoint_specs}{A named list, one entry per covariate to include
in the matching, in the format described in
\code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}}.}

\item{estimator}{\code{"regression"} (the default) or \code{"mean_diff"}; see
Details.}

\item{covariates}{Character vector of covariate names for the
\code{"regression"} estimator, using their raw (uncoarsened) values.
\code{NULL} (the default) uses every non-excluded covariate in \code{matched}
(i.e. \code{names(matched$kinds)} minus any \code{"excluded"} entries) -- the
same covariates that entered the matching. Ignored for
\code{"mean_diff"}.}
}
\value{
A list combining the elements of \code{\link[=elicit_and_match]{elicit_and_match()}}'s output
(under \code{matched}) and \code{\link[=fit_effect]{fit_effect()}}'s output (\code{tau_hat}, \code{var_hat},
\code{n_used}, \code{strat_diff}, \code{unit_tau_hat}, \code{unit_idx}).
}
\description{
Thin composition of \code{\link[=elicit_and_match]{elicit_and_match()}} and \code{\link[=fit_effect]{fit_effect()}} for a single
draw. This is the atomic unit reused, unmodified, by \code{\link[=run_M_draws]{run_M_draws()}} and
by the resampling-based diagnostics (\code{\link[=excess_variance_test]{excess_variance_test()}},
\code{\link[=existence_test]{existence_test()}}).
}
