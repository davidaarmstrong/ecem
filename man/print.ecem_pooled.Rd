% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-methods.R
\name{print.ecem_pooled}
\alias{print.ecem_pooled}
\title{Print a pooled result}
\usage{
\method{print}{ecem_pooled}(x, level = 0.95, digits = 4, ...)
}
\arguments{
\item{x}{An object of class \code{"ecem_pooled"}.}

\item{level}{Confidence level for the interval. Defaults to \code{0.95}.}

\item{digits}{Integer; number of significant digits to print.}

\item{...}{Ignored; present for S3 method consistency.}
}
\value{
\code{x}, invisibly.
}
\description{
Prints the pooled point estimate, its standard error, and a confidence
interval, followed by the components (\code{Wbar}, \code{B}, \code{T}, \code{lambda_hat},
\code{df}) that produced them. Works on the output of \code{\link[=pool_draws]{pool_draws()}},
\code{\link[=pool_rubins_rules]{pool_rubins_rules()}}, or \code{\link[=pool_rubins_rules_exact]{pool_rubins_rules_exact()}} -- whichever
produced \code{x}, "exact" pooling is detected from the presence of \code{K}
(\code{pool_rubins_rules_exact()}'s draw count) rather than requiring the
\code{exact} field that only \code{\link[=pool_draws]{pool_draws()}} sets, and \code{lambda_hat}/\code{df} are
filled in at print time if the object being printed didn't already have
them.
}
\details{
The confidence interval uses a \code{t} reference distribution with the
Barnard-Rubin \code{df} under Monte Carlo pooling, as classical Rubin's
rules would. Under exact pooling \code{df} is \code{NA} -- there is no finite-\code{M}
uncertainty left to adjust for, since every achievable configuration
was actually run -- so the interval instead uses a normal reference
distribution, reflecting only the ordinary sampling uncertainty in
\code{Wbar} and \code{B} (see the paper's appendix remark on the achievable-
configuration ceiling for why a confidence interval remains meaningful
here even though there is no draw-selection uncertainty left).
}
\examples{
pop <- simulate_population(N = 500)
draws <- run_M_draws(pop, "D", "Y", list(age = c(45)), M = 10)
pool_draws(draws)

}
