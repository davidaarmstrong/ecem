% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match-and-fit.R
\name{fit_effect}
\alias{fit_effect}
\title{Estimate the treatment effect on a matched sample}
\usage{
fit_effect(
  data,
  treat_var,
  outcome_var,
  matched,
  estimator = c("regression", "mean_diff"),
  covariates = NULL
)
}
\arguments{
\item{data}{The same data frame passed to \code{\link[=elicit_and_match]{elicit_and_match()}}.}

\item{treat_var}{Character; name of the 0/1 treatment indicator column.}

\item{outcome_var}{Character; name of the outcome column.}

\item{matched}{The list returned by \code{\link[=elicit_and_match]{elicit_and_match()}}.}

\item{estimator}{\code{"regression"} (the default) or \code{"mean_diff"}; see
Details.}

\item{covariates}{Character vector of covariate names for the
\code{"regression"} estimator, using their raw (uncoarsened) values.
\code{NULL} (the default) uses every non-excluded covariate in \code{matched}
(i.e. \code{names(matched$kinds)} minus any \code{"excluded"} entries) -- the
same covariates that entered the matching. Ignored for
\code{"mean_diff"}.}
}
\value{
A list with elements: \code{tau_hat} (the within-draw effect
estimate), \code{var_hat} (its estimated sampling variance), \code{n_used}
(retained sample size), \code{strat_diff} (per-stratum mean differences),
\code{unit_tau_hat} (each retained unit's own stratum diff -- the
operationalized \code{tau(x)} surface used by the covariance diagnostics,
computed the same way regardless of \code{estimator}), and \code{unit_idx} (the
row indices these correspond to).
}
\description{
Two estimators are available, both operating on the matched object
returned by \code{\link[=elicit_and_match]{elicit_and_match()}}:
}
\details{
\itemize{
\item \code{"regression"} (the default): a weighted regression of \code{outcome_var}
on \code{treat_var} plus \code{covariates}, using CEM's own control-weighting
scheme (\code{\link[=cem_weights]{cem_weights()}}) on the retained sample -- the "run whatever
model you would have run anyway, on the matched data" approach the
CEM literature itself recommends (Iacus, King & Porro 2011/2012,
building on Ho, Imai, King & Stuart's (2007) matching-as-
nonparametric-preprocessing rationale). \code{tau_hat}/\code{var_hat} are \code{treat_var}'s
coefficient and squared standard error. Unlike \code{"mean_diff"}, this can
adjust for covariates' \emph{uncoarsened} values, mopping up residual
imbalance that coarsening-into-bins leaves behind -- at the cost of
the usual regression caveats (functional-form assumptions on
\code{covariates}, classical rather than robust standard errors here).
\item \code{"mean_diff"}: a treated-count-weighted stratum mean-difference (no
regression, no covariate adjustment beyond the matching strata
themselves) -- simpler and always well-defined, but forgoes the
efficiency and residual-imbalance adjustment a regression gives.
}

Either way, the per-unit \code{tau(x)} surface used by the covariance
diagnostics (\code{\link[=cov_att_ate]{cov_att_ate()}}, \code{\link[=cov_retention_per_draw]{cov_retention_per_draw()}}) -- returned
here as \code{unit_tau_hat} -- is always the stratum mean-difference: those
diagnostics need a covariate-indexed CATE proxy, which a single
regression coefficient per draw can't provide, so the estimator choice
only changes what's reported as the headline \code{tau_hat}/\code{var_hat}, not
what feeds the diagnostics.
}
