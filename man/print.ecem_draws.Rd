% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-methods.R
\name{print.ecem_draws}
\alias{print.ecem_draws}
\title{Print an ecem_draws object}
\usage{
\method{print}{ecem_draws}(x, digits = 4, ...)
}
\arguments{
\item{x}{An object of class \code{"ecem_draws"}, as returned by
\code{\link[=run_M_draws]{run_M_draws()}}.}

\item{digits}{Integer; number of significant digits to print for
\code{tau_hat}, \code{var_hat}, and \code{weight}.}

\item{...}{Ignored; present for S3 method consistency.}
}
\value{
The per-draw data frame from \code{\link[=as.data.frame.ecem_draws]{as.data.frame.ecem_draws()}},
invisibly.
}
\description{
Prints a short header (number of draws, Monte Carlo vs. exact, and each
covariate's kind) followed by the per-draw table from
\code{\link[=as.data.frame.ecem_draws]{as.data.frame.ecem_draws()}}. Returns that table invisibly, so
\code{tab <- print(draws)} (or, more directly, \code{as.data.frame(draws)})
captures it.
}
\details{
There is deliberately no separate "summary" of these numbers beyond
\code{\link[=summary.ecem_draws]{summary.ecem_draws()}}, which just pools them -- see \code{\link[=pool_draws]{pool_draws()}}.
The individual draws are the thing worth seeing raw; anything you'd
want to summarize about them (a pooled point estimate and variance) is
exactly what pooling already computes.
}
\examples{
pop <- simulate_population(N = 500)
draws <- run_M_draws(pop, "D", "Y", list(age = c(45)), M = 5)
draws

}
