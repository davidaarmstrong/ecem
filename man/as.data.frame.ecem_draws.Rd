% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-methods.R
\name{as.data.frame.ecem_draws}
\alias{as.data.frame.ecem_draws}
\title{Tidy an ecem_draws object into a data frame}
\usage{
\method{as.data.frame}{ecem_draws}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"ecem_draws"}, as returned by
\code{\link[=run_M_draws]{run_M_draws()}}.}

\item{...}{Ignored; present for S3 method consistency.}
}
\value{
A data frame with one row per draw and columns \code{draw} (index),
\code{tau_hat}, \code{var_hat}, \code{n_used}, \code{weight} (uniform \code{1/M} for Monte
Carlo draws, generally unequal for exact enumeration -- see
\code{\link[=run_M_draws]{run_M_draws()}}'s \code{exact_if_K_leq}), and, for any covariate specified
via \code{\link[=regime]{regime()}}, a \verb{regime_<covariate>} column giving which regime
that draw used.
}
\description{
Extracts one row per draw from an \link[=run_M_draws]{ecem_draws} object: the
individual \code{tau_hat} and \code{var_hat} estimates that \code{\link[=pool_draws]{pool_draws()}} would
otherwise combine into a single pooled number. Useful when you want the
draw-level detail directly -- e.g. to plot the distribution of
\code{tau_hat} across draws, or to hand to your own pooling/diagnostic code.
}
\examples{
pop <- simulate_population(N = 500)
draws <- run_M_draws(pop, "D", "Y", list(age = c(45)), M = 5)
as.data.frame(draws)

}
