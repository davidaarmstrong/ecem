% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pooling.R
\name{pool_rubins_rules}
\alias{pool_rubins_rules}
\title{Pool Monte Carlo draws via Rubin's rules}
\usage{
pool_rubins_rules(draws)
}
\arguments{
\item{draws}{A list of per-draw records, as returned by \code{\link[=run_M_draws]{run_M_draws()}}
(with \code{exact_if_K_leq} left at its default \code{NULL}) or by repeated
calls to \code{\link[=run_draw]{run_draw()}}.}
}
\value{
An object of class \code{"ecem_pooled"} (see \code{\link[=print.ecem_pooled]{print.ecem_pooled()}}):
a list with elements \code{tau_bar}, \code{Wbar}, \code{B}, \code{T}, \code{lambda_hat}, \code{df},
and \code{M}.
}
\description{
Flat, single-level Rubin's rules pooling across a set of Monte Carlo
draws produced by \code{\link[=run_M_draws]{run_M_draws()}}. Assumes equal weight across draws --
if \code{draws} came from exact enumeration (unequal weights), use
\code{\link[=pool_draws]{pool_draws()}} instead, which dispatches correctly either way.
}
\section{Caveat on \code{df}}{

The Barnard-Rubin small-sample degrees-of-freedom adjustment needs a
"complete-data" degrees of freedom (\code{df_com}) from the estimator used
within each draw. This function uses \code{mean(n_used) - 2} as a
placeholder; replace it with something specific to your estimator before
relying on \code{df} for actual inference.
}

