% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enumeration.R
\name{enumerate_configs}
\alias{enumerate_configs}
\title{Enumerate every achievable configuration exactly}
\usage{
enumerate_configs(data, cutpoint_specs)
}
\arguments{
\item{data}{A data frame containing \code{treat_var} and every covariate
named in \code{cutpoint_specs}.}

\item{cutpoint_specs}{A named list, one entry per covariate to include
in the matching, in the format described in
\code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}}.}
}
\value{
A list with elements \code{specs} (list of \code{K} realized
\code{cutpoint_specs}), \code{weights} (numeric, summing to 1), and \code{regimes}
(list of named lists giving the drawn regime, if any, per config).
}
\description{
See \code{?"exact-enumeration"} for the underlying argument. Returns \code{K}
exact \code{cutpoint_specs} (each with elicited ranges replaced by a single
realized, fixed cutpoint vector -- \code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}} already
treats a plain numeric vector as fixed, so \code{\link[=run_draw]{run_draw()}} needs no changes
to consume these) plus their exact probability weights, proportional to
interval width under the continuous elicited prior (and, for a
\code{\link[=regime]{regime()}} covariate, to the regime's own prior weight as well). Also
returns, per config, which regime (if any) each varying regime
covariate landed in.
}
