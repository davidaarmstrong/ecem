% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{cov_att_ate}
\alias{cov_att_ate}
\title{ATT-ATE labeling diagnostic: Cov(tau_hat(X), p_hat(X))}
\usage{
cov_att_ate(tau_i, p_hat, treat_idx)
}
\arguments{
\item{tau_i}{Numeric vector, the pooled per-unit \code{tau(x)} surface from
\code{\link[=pooled_unit_tau]{pooled_unit_tau()}}.}

\item{p_hat}{Numeric vector of propensity scores from
\code{\link[=fit_propensity]{fit_propensity()}}, same length and indexing as \code{tau_i}.}

\item{treat_idx}{Integer vector of row indices for the treated units.}
}
\value{
A single numeric covariance estimate.
}
\description{
A single number (unlike \code{\link[=cov_retention_per_draw]{cov_retention_per_draw()}}), since \code{p(x)} does
not depend on the coarsening draw. A nonzero estimate does not
invalidate the pooled estimate; it indicates the pooled FSATT should be
labeled ATT rather than ATE (see the paper's Proposition on the ATT-ATE
gap).
}
