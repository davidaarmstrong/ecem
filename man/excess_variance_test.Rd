% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/congeniality-tests.R
\name{excess_variance_test}
\alias{excess_variance_test}
\title{Excess-variance bootstrap test on the realized draws}
\usage{
excess_variance_test(
  data,
  treat_var,
  outcome_var,
  cutpoint_specs,
  M,
  tau_bar,
  B_obs,
  n_boot = 200,
  estimator = c("regression", "mean_diff"),
  covariates = NULL,
  progress = interactive()
)
}
\arguments{
\item{data}{A data frame containing \code{treat_var} and every covariate
named in \code{cutpoint_specs}.}

\item{treat_var}{Character; name of the 0/1 treatment indicator column
in \code{data}.}

\item{outcome_var}{Character; name of the outcome column in \code{data}.}

\item{cutpoint_specs}{A named list, one entry per covariate to include
in the matching, in the format described in
\code{\link[=draw_cutpoints_for_var]{draw_cutpoints_for_var()}}.}

\item{M}{Integer; number of draws to rerun within each bootstrap
replicate (should match the \code{M} used to compute \code{B_obs}).}

\item{tau_bar}{The pooled point estimate from \code{\link[=pool_rubins_rules]{pool_rubins_rules()}} or
\code{\link[=pool_draws]{pool_draws()}}.}

\item{B_obs}{The observed between-draw variance from the same pooling
call.}

\item{n_boot}{Integer; number of bootstrap replicates. Kept small in
examples for speed; use at least a few hundred for actual inference.}

\item{estimator, covariates}{Passed straight through to \code{\link[=fit_effect]{fit_effect()}}
for every refit inside the bootstrap. Should match whatever \code{draws}
(or whatever produced \code{tau_bar}/\code{B_obs}) was actually computed with --
refitting under a different estimator than the real draws used would
make this test answer a different question than the one its \code{B_obs}
was computed for. If you have a \code{draws} object, prefer calling this
indirectly through \code{\link[=pooling_diagnostics]{pooling_diagnostics()}}, which recovers these from
\code{draws}'s attributes automatically rather than requiring you to repeat
them here.}

\item{progress}{Logical; show a text progress bar over the \code{n_boot}
bootstrap replicates (each of which reruns all \code{M} draws). Defaults to
\code{interactive()}, so it stays quiet in scripts, \verb{R CMD check}, and
\code{testthat} runs. Written to \code{stderr()} via \code{\link[utils:txtProgressBar]{utils::txtProgressBar()}},
so it never interferes with captured \code{stdout} output.}
}
\value{
A list with elements \code{p_value}, \code{ratio} (\code{B_obs} divided by the
median of the null distribution), and \code{B_null_dist} (the full vector
of bootstrapped \verb{B*} values).
}
\description{
Tests whether the observed between-draw variance \code{B} exceeds what
sampling noise alone predicts, computed directly on the realized draws
(as opposed to \code{\link[=flatness_test_XE]{flatness_test_XE()}}, which is a proxy computed on the
full retained sample before matching). Forces the null
\eqn{FSATT_1 = \dots = FSATT_M} onto the data by substituting the pooled
\code{tau_bar} for every unit's draw-specific contribution
(\eqn{Y^* = Y - \bar\tau D}), resamples rows with replacement, reruns
all \code{M} draws on that same resample, and records the resulting
between-draw variance \verb{B*}. Repeating this traces out \code{B}'s null
distribution under exact congeniality -- correlation across draws is
preserved, since the resample is shared across all draws within a given
replicate -- without ever estimating or inverting a covariance matrix.
}
\examples{
\donttest{
set.seed(1)
pop <- simulate_population(N = 500)
specs <- list(age = list(c(25, 33), c(60, 68)), educ = c(12, 16))
draws <- run_M_draws(pop, "D", "Y", specs, M = 10)
pooled <- pool_rubins_rules(draws)
excess_variance_test(pop, "D", "Y", specs, M = 10,
                      tau_bar = pooled$tau_bar, B_obs = pooled$B,
                      n_boot = 20)
}

}
