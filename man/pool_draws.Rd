% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pooling.R
\name{pool_draws}
\alias{pool_draws}
\title{Pool a set of draws, dispatching to the correct formula}
\usage{
pool_draws(draws)
}
\arguments{
\item{draws}{A list of per-draw records, as returned by \code{\link[=run_M_draws]{run_M_draws()}}.}
}
\value{
An object of class \code{"ecem_pooled"} (see \code{\link[=print.ecem_pooled]{print.ecem_pooled()}}):
a list with elements \code{tau_bar}, \code{Wbar}, \code{B}, \code{T}, \code{lambda_hat}, \code{df},
and \code{exact} (logical, whether exact pooling was used).
}
\description{
Pools a \code{draws} object from \code{\link[=run_M_draws]{run_M_draws()}}, dispatching to the exact
(\code{\link[=pool_rubins_rules_exact]{pool_rubins_rules_exact()}}) or Monte Carlo (\code{\link[=pool_rubins_rules]{pool_rubins_rules()}})
formula depending on how the draws were produced. This is the function
to call by default -- it returns the same set of fields either way, so
nothing downstream needs to know or care which path \code{run_M_draws()}
took.
}
\details{
\code{df} is a Barnard-Rubin quantity motivated by finite-\code{M}
multiple-imputation uncertainty: how much is left to learn from drawing
more configurations. Once every achievable configuration has actually
been enumerated there is none left, so \code{df} comes back \code{NA} under exact
pooling -- \code{lambda_hat} (still computed as \code{B / T}, the share of total
variance coming from between-draw variation) and \code{B}/\code{T} themselves
(exact rather than Monte Carlo estimates in that case) remain
meaningful and are what \code{\link[=print.ecem_pooled]{print.ecem_pooled()}} falls back to a normal,
rather than a \code{t}, reference distribution for.
}
