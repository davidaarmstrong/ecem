% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-methods.R
\name{print.ecem_label_diagnostics}
\alias{print.ecem_label_diagnostics}
\title{Print a label-diagnostics result}
\usage{
\method{print}{ecem_label_diagnostics}(x, digits = 4, ...)
}
\arguments{
\item{x}{An object of class \code{"ecem_label_diagnostics"}.}

\item{digits}{Integer; number of significant digits to print.}

\item{...}{Ignored; present for S3 method consistency.}
}
\value{
\code{x}, invisibly.
}
\description{
Reports the ATT-ATE term (\code{Cov(tau_hat(X), p_hat(X))}, and the
equivalent ATT - ATE gap) and the FSATT-ATT term
(\eqn{\overline{\mathrm{Gap}}_r}, mean and spread across draws) from
\code{\link[=label_diagnostics]{label_diagnostics()}}, followed by a \emph{suggested} FSATT/ATT/ATE label --
explicitly flagged here as a heuristic this package proposes (see
\code{\link[=label_diagnostics]{label_diagnostics()}}'s Details), not a threshold the paper itself
specifies; the paper deliberately leaves "appreciable" to the analyst.
}
\examples{
\donttest{
set.seed(1)
pop <- simulate_population(N = 1000, heterogeneous = TRUE)
specs <- list(age = list(c(25, 33), c(60, 68)), educ = c(12, 16))
draws <- run_M_draws(pop, "D", "Y", specs, M = 15)
label_diagnostics(pop, draws)
}

}
