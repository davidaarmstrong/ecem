% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{retention_matrix}
\alias{retention_matrix}
\title{Build the retention matrix R_m(X_i)}
\usage{
retention_matrix(draws, treat_idx)
}
\arguments{
\item{draws}{A list of per-draw records, as returned by \code{\link[=run_M_draws]{run_M_draws()}}.}

\item{treat_idx}{Integer vector of row indices for the treated units
(e.g. \code{which(data$treat_var == 1)}).}
}
\value{
An integer matrix with one row per treated unit and one column
per draw, entries 0/1.
}
\description{
For every treated unit and every draw, records whether that unit was
retained (matched to at least one control in its stratum) in that draw.
}
