% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-methods.R
\name{summary.ecem_draws}
\alias{summary.ecem_draws}
\title{Summarize an ecem_draws object by pooling it}
\usage{
\method{summary}{ecem_draws}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"ecem_draws"}, as returned by
\code{\link[=run_M_draws]{run_M_draws()}}.}

\item{...}{Ignored; present for S3 method consistency.}
}
\value{
The list returned by \code{\link[=pool_draws]{pool_draws()}}: \code{tau_bar}, \code{Wbar}, \code{B},
\code{T}, \code{lambda_hat}, \code{df}, \code{exact}.
}
\description{
A thin wrapper around \code{\link[=pool_draws]{pool_draws()}}: the individual draws in an
\link[=run_M_draws]{ecem_draws} object don't have a separate "summary" beyond
their pooled point estimate and variance, so \code{summary()} just returns
that. See \code{\link[=print.ecem_draws]{print.ecem_draws()}} / \code{\link[=as.data.frame.ecem_draws]{as.data.frame.ecem_draws()}} for the
individual \code{tau_hat}/\code{var_hat} values this pools together.
}
\examples{
pop <- simulate_population(N = 500)
draws <- run_M_draws(pop, "D", "Y", list(age = c(45)), M = 5)
summary(draws)

}
