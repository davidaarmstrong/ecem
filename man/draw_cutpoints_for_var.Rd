% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutpoint-specs.R
\name{draw_cutpoints_for_var}
\alias{draw_cutpoints_for_var}
\title{Resolve one covariate's cutpoint spec for a single draw}
\usage{
draw_cutpoints_for_var(spec)
}
\arguments{
\item{spec}{One covariate's cutpoint spec, as described above.}
}
\value{
A list with element \code{kind} (one of \code{"excluded"}, \code{"exact"},
\code{"elicited"}, \code{"fixed"}, \code{"regime"}), \code{cutpoints} (the realized numeric
cutpoints, or \code{NULL} for \code{"excluded"}/\code{"exact"}), and, for \code{kind == "regime"}, \code{regime} giving the name of the regime that was drawn.
}
\description{
Internal building block of \code{\link[=elicit_and_match]{elicit_and_match()}}. A cutpoint spec for one
covariate is one of five things:
}
\details{
\itemize{
\item a list of numeric ranges, e.g. \code{list(c(25, 33), c(60, 68))}:
\strong{elicited} (\code{X_E}). Draws one value uniformly, continuously, within
each range (a list with k ranges produces k cutpoints, i.e. k+1 bins).
This is a genuine continuous draw, not snapped to any observed data
value -- snapping to observed values would distort the prior toward
wherever the data happens to be dense.
\item a plain numeric vector, e.g. \code{c(12, 16)}: \strong{fixed} (\code{X_F}). Used as
is, identical across every draw. Also used internally by exact
enumeration to pass a single realized cutpoint vector through
unchanged (see \code{\link[=enumerate_configs]{enumerate_configs()}}).
\item the string \code{"exact"}: matches on the raw, uncoarsened value of this
covariate (no binning at all).
\item \code{NULL}: \strong{excluded}. This covariate does not enter the matching
specification at all.
\item a \code{\link[=regime]{regime()}} object: two or more competing elicited cutpoint sets;
see \code{\link[=regime]{regime()}} for the semantics.
}
}
\keyword{internal}
